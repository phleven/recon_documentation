Create PostgreSQL Container

1. Copy docker-compose.yml to C:\Dev\biq-ai-recon-schema_docker_compose

2. Start Docker Desktop

3. Open terminal
   a. cd C:\Dev\biq-ai-recon-schema_docker_compose
   b. docker compose up -d
   c. docker compose logs
   d. Check for new containers in Docker Desktop
      1. biq-ai-recon-schema_docker_compose  
         a. postgres
         b. zookeeper
         c. kafka
         d. kafka-connect
  